apiVersion: storage.platform.ohanyere.io/v1alpha1
kind: S3BucketClaim
metadata:
  name: calendar-api-bucket
  labels:
    app.kubernetes.io/name: calendar-api
    finops.ohanyere.io/cost-center: commerce
spec:
  environment: dev
  versioning: true
  encryption: platform-managed
  lifecycle:
    transitionToInfrequentAccessDays: 30
