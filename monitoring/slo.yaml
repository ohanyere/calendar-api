apiVersion: sre.platform.ohanyere.io/v1alpha1
kind: ServiceLevelObjective
metadata:
  name: calendar-api-availability
  labels:
    app.kubernetes.io/name: calendar-api
    finops.ohanyere.io/cost-center: commerce
spec:
  service: calendar-api
  owner: Commerce
  objective: 99.9
  window: 30d
  indicator:
    type: availability
    goodEvents: sum(rate(http_requests_total{service="calendar-api",status!~"5.."}[5m]))
    totalEvents: sum(rate(http_requests_total{service="calendar-api"}[5m]))
  alerting:
    burnRate:
      enabled: true
