apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: calendar-api
  labels:
    app.kubernetes.io/name: calendar-api
spec:
  hosts:
    - calendar-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: calendar-api
            port:
              number: 80
          weight: 100
